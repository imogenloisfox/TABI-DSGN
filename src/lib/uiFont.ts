/** Primary UI face — ABC Diatype Bold (trial). Use with `font-bold` where the file is the only weight. */
export const UI_FONT_FAMILY = '"ABC Diatype", ui-sans-serif, system-ui, sans-serif' as const;

export const uiFontStyle = { fontFamily: UI_FONT_FAMILY } as const;
